describe('adaptive.motion', function() {

  beforeEach(module('adaptive.motion'));

  var rootscope;
  beforeEach(inject(function($rootScope) {
    rootScope = $rootScope;
  }));

  var elm, scope;


  it('should do nothing', function(){

  });

});